language: rust
# disable until we get graceful failures in cargo install
#cache: cargo
sudo: false

branches:
  only:
    - master

install:
  - curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.8/install.sh | bash
  - source ~/.nvm/nvm.sh
  - nvm install 10

before_script:
  - npm install -g firebase-tools 
  - cargo install mdbook

script:
  - mdbook build

after_success:
  - firebase deploy --token=$FIREBASE_TOKEN
